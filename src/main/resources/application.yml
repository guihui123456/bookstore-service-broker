management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

---
spring:
  profiles:
    active: cloud
  datasource:
    url: jdbc:mysql://10.12.30.39:3306/brokerdb
    username: root
    password: root